// P11 owns this file. Contract: see ARCHITECTURE.md.
// createMenus(el, game) -> { open(), close() }
// Main menu, pause, results, controls help. Sim-racer styling.

export function createMenus(el, game) {
  return {
    open() {},
    close() {},
  };
}
