// P11 owns this file. Contract: see ARCHITECTURE.md.
// createHud(el) -> { update(state) }
// Speedo, gear, rpm bar, lap/position, lap times + delta, minimap, messages.

export function createHud(el) {
  return {
    update(state) {},
  };
}
