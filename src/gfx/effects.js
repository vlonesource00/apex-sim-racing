// P10 owns this file. Contract: see ARCHITECTURE.md.
// createEffects(scene) -> { update(cars, dt) }
// Tire smoke, persistent skidmarks, sparks, brake glow, speed sense FX.

export function createEffects(scene) {
  return {
    update(cars, dt) {},
  };
}
