// P8 owns this file. Contract: see ARCHITECTURE.md.
// buildEnvironment(track, scene) -> env
// Sky, sun/atmosphere polish, textured asphalt/grass/curbs, walls+barriers,
// grandstands, trees, start gantry, trackside props. Instanced where possible.

export function buildEnvironment(track, scene) {
  return {};
}
